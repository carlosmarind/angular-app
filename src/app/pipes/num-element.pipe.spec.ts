import { NumElementPipe } from './num-element.pipe';

describe('NumElementPipe', () => {
  it('create an instance', () => {
    const pipe = new NumElementPipe();
    expect(pipe).toBeTruthy();
  });
});
